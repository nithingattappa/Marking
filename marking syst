#include<stdio.h>
#include<conio.h>
main()
{
int a,b,c,total:
printf("enter the marks for a");
scanf("%d",a);
printf("enter the marks for b"):
scanf("%d",b);
printf("enter the marks for c");
scanf("%d",c);
total=a+b+c;
if(total<60)
{
printf("Average");
}
else
{
printf("high");
}
getch();
}
